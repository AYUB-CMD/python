#tuples
#creating a tuple using()
t = (1, 2, 4, 5)
#creating empty tuple
t = ()
#creating single tuple but end with , comma
t=(4,)
#printing element of tuple
#print(t[0])
#cannot update the values of a tuple
#t[4] = 6
#print(t)


#tuples methods
tup = (1, 2, 4, 5, 1)
print(tup.count(1))  #count the number how many 1 comes in that tuples
print(tup.index(5)) #finds the where is 5 in that tuples index